@model IEnumerable<Garage_2.Models.ParkedVehicle>

@{
    ViewData["Title"] = "Index";
}

<h1>All parked vehicles</h1>

<form asp-action="Filter">
    <div class="input-group mb-3">
        <input type="text" class="form-control" name="regNrSearch" placeholder="Search on reg.nr..." />
        @*<div class="input-group-append">
            <select asp-for="Genre" asp-items="@Model.Genres" class="form-control">
                <option selected="selected" value="">Please select</option>
            </select>
        </div>*@
        <div class="input-group-append">
            <button type="submit" class="btn btn-primary"><i class="fa fa-search">&emsp;Search</i></button>
        </div>
    </div>
</form>

@*<p>*@
    <form asp-action="Create">       
        <input type="submit" value="Park Vehicle" class="btn btn-danger float-md-left" />
    </form>
    @*<input asp-action="Create" type="submit" value="Park Vehicle" class="btn btn-primary" />*@
    @*<a asp-action="Create">Park a car</a>*@
@*</p>*@
    <form asp-action="Overview">
        <input type="submit" value="Overview" class="btn btn-warning float-md-right"/>
    </form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegNr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NrOfWheels)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeOfArrival)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegNr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NrOfWheels)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeOfArrival)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Receipt" asp-route-id="@item.Id">Checkout</a>
            </td>
        </tr>
}
    </tbody>
</table>
